apiVersion: v1
kind: Service
metadata:
  name: {{ include "rclone-s3.fullname" . }}
  labels:
    {{- include "rclone-s3.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    {{- include "rclone-s3.labels" . | nindent 4 }}
  ports:
    - name: s3
      port: 8080
      targetPort: s3
