apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: {{ include "matrix-redirect.fullname" . }}
spec:
  redirectRegex:
    regex: {{ .Values.redirect.regex | quote }}
    replacement: {{ .Values.redirect.replacement | quote }}
    permanent: {{ .Values.redirect.permanent }}
